version: '3.8'

services:
  web:
    build: .
    container_name: trabalho_castelo_app
    ports:
      - "5000:5000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3